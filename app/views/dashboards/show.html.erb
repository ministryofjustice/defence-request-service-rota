<h2>Dashboard</h1>

<% if current_user.name.present? %>
  <p>
    Welcome to your Defence Service Rota dashboard, <%= current_user.name %>.
    <% if current_user.email.present? %>
      <small>Logged in as: <%= current_user.email %></small>
    <% end %>
  </p>
<% else %>
  <p>
    Welcome to your Defence Service Rota dashboard.
  </p>
<% end %>

<%= link_to "Procurement areas", procurement_areas_path %>

<h2>Solicitors</h2>
<%= render @dashboard.solicitors %>

<h2>Organisations</h2>
<%= render @dashboard.organisations %>
