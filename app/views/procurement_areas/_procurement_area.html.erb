<div>
  <h3><%= procurement_area.name %></h3>

  <div>
    <%= link_to "Add procurement area member", new_procurement_area_membership_path(procurement_area_id: procurement_area.id) %>
    <%= link_to "Edit", edit_procurement_area_path(procurement_area) %>
    <%= link_to "Delete", procurement_area_path(procurement_area), method: :delete, data: { confirm: "Are you sure?" } %>
  </div>
</div>
